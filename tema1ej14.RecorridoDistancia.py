'''
distancia_recorrido = int (input('Ingrese su distancia en KM'))
km_por_hora_pie = 0.25 * 60 
km_por_hora_auto = 0.04 * 60
km_por_hora_bici = 0.125 * 60
resultado_pie = distancia_recorrido * km_por_hora_pie
resultado_auto = distancia_recorrido * km_por_hora_auto
resultado_bici = distancia_recorrido * km_por_hora_bici
print(resultado_pie)
print(resultado_auto)
print(resultado_bici)
'''

# este no me salio al parecer.
# No modificar, solo cuando el profe corrija, yo escribo de vuelta el codigo.

distancia_recorrido = int (input('Ingrese su distancia en KM'))
km_por_hora_pie = 25 / 60 
km_por_hora_auto = 4 / 60
km_por_hora_bici = 12.5 / 60
resultado_pie = distancia_recorrido * km_por_hora_pie
resultado_auto = distancia_recorrido * km_por_hora_auto
resultado_bici = distancia_recorrido * km_por_hora_bici
print(resultado_pie)
print(resultado_auto)
print(resultado_bici)




